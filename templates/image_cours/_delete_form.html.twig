<form method="post" action="{{ path('app_image_cours_delete', {'id': image_cour.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ image_cour.id) }}">
    <button class="btn">Delete</button>
</form>
